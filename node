var http = require("http")

http.createServer(function(req, res) {
    if (req.method == "POST") {
        res.writeHead(200)
        res.end("method is post")
    } else{
        res.writeHead(200)
        res.end("method is " + req.method.toString())
    }
})
